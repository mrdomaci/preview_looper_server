name: Curl Request

on:
  push:
    branches:
      - main

jobs:
  curl-request:
    runs-on: ubuntu-latest

    steps:
      - name: Make cURL request
        run: curl -k ${{ secrets.DEPLOY_URL }}<YOUR_URL>